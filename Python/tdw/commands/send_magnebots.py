# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.send_data_command import SendDataCommand
from typing import List


class SendMagnebots(SendDataCommand):
    """
    Send data for each Magnebot in the scene.
    """

    def __init__(self, ids: List[int] = None, frequency: str = "once"):
        """
        :param ids: The IDs of the Magnebots. If this list is undefined or empty, the build will return data for all Magnebots.
        :param frequency: The frequency at which data is sent.
        """

        super().__init__(frequency=frequency)
        if ids is None:
            """:field
            The IDs of the Magnebots. If this list is undefined or empty, the build will return data for all Magnebots.
            """
            self.ids: List[int] = []
        else:
            self.ids = ids
