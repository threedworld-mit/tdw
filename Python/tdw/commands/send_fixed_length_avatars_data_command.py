# AUTOGENERATED FROM C#. DO NOT MODIFY.

from abc import ABC
from tdw.commands.send_fixed_length_data_command import SendFixedLengthDataCommand
from typing import List


class SendFixedLengthAvatarsDataCommand(SendFixedLengthDataCommand, ABC):
    """
    Abstract base class for sending avatar data where the size of the output data is the same on every communicate call.
    """

    def __init__(self, ids: List[str] = None, frequency: str = "once"):
        """
        :param ids: The avatar IDs. If empty, data for all avatars in the scene will be sent.
        :param frequency: The frequency at which data is sent.
        """

        super().__init__(frequency=frequency)
        if ids is None:
            """:field
            The avatar IDs. If empty, data for all avatars in the scene will be sent.
            """
            self.ids: List[str] = []
        else:
            self.ids = ids
