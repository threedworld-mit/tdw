# AUTOGENERATED FROM C#. DO NOT MODIFY.

from abc import ABC
from tdw.commands.send_multiple_data_once_command import SendMultipleDataOnceCommand
from typing import Dict


class SendRaycastCommand(SendMultipleDataOnceCommand, ABC):
    """
    These commands cast different types of rays and send the results to the controller.
    """

    def __init__(self, origin: Dict[str, float] = None, destination: Dict[str, float] = None, id: int = 0):
        """
        :param origin: The origin of the raycast.
        :param destination: The destination of the raycast.
        :param id: The ID of the output data object. This can be used to match the output data back to the command that created it.
        """

        super().__init__(id=id)
        if origin is None:
            """:field
            The origin of the raycast.
            """
            self.origin: Dict[str, float] = {"x": 0, "y": 0, "z": 0}
        else:
            self.origin = origin
        if destination is None:
            """:field
            The destination of the raycast.
            """
            self.destination: Dict[str, float] = {"x": 1, "y": 1, "z": 1}
        else:
            self.destination = destination
