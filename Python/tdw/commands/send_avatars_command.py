# AUTOGENERATED FROM C#. DO NOT MODIFY.

from abc import ABC
from tdw.commands.send_data_command import SendDataCommand
from typing import List


class SendAvatarsCommand(SendDataCommand, ABC):
    """
    These commands send data about avatars in the scene.
    """

    def __init__(self, ids: List[str] = None, frequency: str = "once"):
        """
        :param ids: The IDs of the avatars. If this list is undefined or empty, the build will return data for all avatars.
        :param frequency: The frequency at which data is sent.
        """

        super().__init__(frequency=frequency)
        if ids is None:
            """:field
            The IDs of the avatars. If this list is undefined or empty, the build will return data for all avatars.
            """
            self.ids: List[str] = []
        else:
            self.ids = ids
