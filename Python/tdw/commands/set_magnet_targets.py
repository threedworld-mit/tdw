# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.magnebot_command import MagnebotCommand
from tdw.replicant.arm import Arm
from typing import List


class SetMagnetTargets(MagnebotCommand):
    """
    Set the objects that the Magnebot magnet will try to pick up.
    """

    def __init__(self, arm: Arm = Arm.left, targets: List[int] = None, id: int = 0):
        """
        :param arm: The magnet's arm.
        :param targets: The IDs of the target objects. If a magnet collides with an object, it'll "stick" only if the object ID is in this list. If the magnet is holding an object not in this list, it will continue to do hold it.
        :param id: The ID of the robot in the scene.
        """

        super().__init__(id=id)
        """:field
        The magnet's arm.
        """
        self.arm: Arm = arm
        if targets is None:
            """:field
            The IDs of the target objects. If a magnet collides with an object, it'll "stick" only if the object ID is in this list. If the magnet is holding an object not in this list, it will continue to do hold it.
            """
            self.targets: List[int] = []
        else:
            self.targets = targets
