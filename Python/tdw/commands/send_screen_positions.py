# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.send_avatars_command import SendAvatarsCommand
from typing import Dict, List


class SendScreenPositions(SendAvatarsCommand):
    """
    Given a list of worldspace positions, return the screenspace positions according to each of the avatar's camera.
    """

    def __init__(self, positions: List[Dict[str, float]], position_ids: List[int], ids: List[str] = None, frequency: str = "once"):
        """
        :param positions: The worldspace positions.
        :param position_ids: The unique IDs of each screen position output data. Use this to map the output data to the original worldspace position.
        :param ids: The IDs of the avatars. If this list is undefined or empty, the build will return data for all avatars.
        :param frequency: The frequency at which data is sent.
        """

        super().__init__(ids=ids, frequency=frequency)
        """:field
        The unique IDs of each screen position output data. Use this to map the output data to the original worldspace position.
        """
        self.position_ids: List[int] = position_ids
        """:field
        The worldspace positions.
        """
        self.positions: List[Dict[str, float]] = positions
