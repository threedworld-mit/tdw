# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.robot_joint_command import RobotJointCommand
from tdw.physics_audio.impact_material import ImpactMaterial
from tdw.physics_audio.scrape_material import ScrapeMaterial


class ClatterizeRobotJoint(RobotJointCommand):
    """
    Make a robot respond to Clatter audio by setting its audio values and adding a ClatterObject component. You must send ClatterizeObject for each robot prior to sending InitializeClatter (though they can all be in the same list of commands).
    """

    def __init__(self, joint_id: int, fake_mass: float, resonance: float, amp: float, size: int, impact_material: ImpactMaterial, has_scrape_material: bool = False, scrape_material: ScrapeMaterial = ScrapeMaterial.ceramic, set_fake_mass: bool = False, id: int = 0):
        """
        :param joint_id: The ID of the joint.
        :param fake_mass: If set_fake_mass == True, this is the fake mass, which will be used for audio synthesis instead of the true mass.
        :param resonance: The resonance value (0-1).
        :param amp: The audio amplitude (0-1).
        :param size: The size bucket value (0-5); smaller objects should use smaller values.
        :param impact_material: The impact material. See: tdw.physics_audio.audio_material (which is the same thing as an impact material).
        :param has_scrape_material: If true, the object has a scrape material.
        :param scrape_material: The object's scrape material. Ignored if has_scrape_material == False. See: tdw.physics_audio.scrape_material
        :param set_fake_mass: If True, set a fake audio mass (see below).
        :param id: The ID of the robot in the scene.
        """

        super().__init__(joint_id=joint_id, id=id)
        """:field
        The impact material. See: tdw.physics_audio.audio_material (which is the same thing as an impact material).
        """
        self.impact_material: ImpactMaterial = impact_material
        """:field
        The size bucket value (0-5); smaller objects should use smaller values.
        """
        self.size: int = size
        """:field
        The audio amplitude (0-1).
        """
        self.amp: float = amp
        """:field
        The resonance value (0-1).
        """
        self.resonance: float = resonance
        """:field
        If true, the object has a scrape material.
        """
        self.has_scrape_material: bool = has_scrape_material
        """:field
        The object's scrape material. Ignored if has_scrape_material == False. See: tdw.physics_audio.scrape_material
        """
        self.scrape_material: ScrapeMaterial = scrape_material
        """:field
        If True, set a fake audio mass (see below).
        """
        self.set_fake_mass: bool = set_fake_mass
        """:field
        If set_fake_mass == True, this is the fake mass, which will be used for audio synthesis instead of the true mass.
        """
        self.fake_mass: float = fake_mass
