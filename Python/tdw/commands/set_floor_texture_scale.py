# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.floor_command import FloorCommand
from typing import Dict


class SetFloorTextureScale(FloorCommand):
    """
    Set the scale of the tiling of the floor material's main texture.
    """

    def __init__(self, scale: Dict[str, float] = None):
        """
        :param scale: The tiling scale of the material. Generally (but by no means always), the default tiling scale of a texture is {"x": 1, "y": 1}
        """

        super().__init__()
        if scale is None:
            """:field
            The tiling scale of the material. Generally (but by no means always), the default tiling scale of a texture is {"x": 1, "y": 1}
            """
            self.scale: Dict[str, float] = {"x": 1, "y": 1}
        else:
            self.scale = scale
