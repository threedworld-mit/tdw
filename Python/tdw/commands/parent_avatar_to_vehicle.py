# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.vehicle_command import VehicleCommand
from typing import Dict


class ParentAvatarToVehicle(VehicleCommand):
    """
    Parent an avatar to the vehicle. Usually you'll want to do this to add a camera to the vehicle.
    """

    def __init__(self, id: int, avatar_id: str = "a", position: Dict[str, float] = None):
        """
        :param id: The unique object ID.
        :param avatar_id: The ID of the avatar. It must already exist in the scene.
        :param position: The camera position.
        """

        super().__init__(id=id)
        """:field
        The ID of the avatar. It must already exist in the scene.
        """
        self.avatar_id: str = avatar_id
        if position is None:
            """:field
            The camera position.
            """
            self.position: Dict[str, float] = {"x": 0, "y": 0, "z": 0}
        else:
            self.position = position
