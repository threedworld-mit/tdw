# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.nav_mesh_command import NavMeshCommand
from typing import List


class BakeNavMesh(NavMeshCommand):
    """
    Bake the NavMesh, enabling Unity pathfinding. This must be sent before any other Nav Mesh Commands, and after creating the scene environment (e.g. the procedurally generated room).
    """

    def __init__(self, voxel_size: float = 0.1666667, carve_type: str = "all", ignore: List[int] = None):
        """
        :param voxel_size: The voxel size. A lower value means higher fidelity and a longer bake.
        :param carve_type: How each object in the scene will "carve" holes in the NavMesh.
        :param ignore: A list of object or robot IDs that will be ignored when baking the NavMesh.
        """

        super().__init__()
        """:field
        The voxel size. A lower value means higher fidelity and a longer bake.
        """
        self.voxel_size: float = voxel_size
        """:field
        How each object in the scene will "carve" holes in the NavMesh.
        """
        self.carve_type: str = carve_type
        if ignore is None:
            """:field
            A list of object or robot IDs that will be ignored when baking the NavMesh.
            """
            self.ignore: List[int] = [0]
        else:
            self.ignore = ignore
