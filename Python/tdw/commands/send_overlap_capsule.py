# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.send_overlap_command import SendOverlapCommand
from typing import Dict


class SendOverlapCapsule(SendOverlapCommand):
    """
    Check which objects a capsule-shaped space overlaps with.
    """

    def __init__(self, position: Dict[str, float], radius: float, end: Dict[str, float], id: int = 0):
        """
        :param position: The center of the shape.
        :param radius: The radius of the capsule.
        :param end: The top of the capsule. (The position parameter is the bottom of the capsule).
        :param id: The ID of the output data object. This can be used to match the output data back to the command that created it.
        """

        super().__init__(position=position, id=id)
        """:field
        The top of the capsule. (The position parameter is the bottom of the capsule).
        """
        self.end: Dict[str, float] = end
        """:field
        The radius of the capsule.
        """
        self.radius: float = radius
