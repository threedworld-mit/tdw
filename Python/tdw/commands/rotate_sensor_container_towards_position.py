# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.rotate_sensor_container_towards import RotateSensorContainerTowards
from typing import Dict


class RotateSensorContainerTowardsPosition(RotateSensorContainerTowards):
    """
    Rotate the sensor container towards a position at a given angular speed per frame.
    """

    def __init__(self, position: Dict[str, float] = None, speed: float = 3, sensor_name: str = "SensorContainer", avatar_id: str = "a"):
        """
        :param position: The target position to rotate towards.
        :param speed: The maximum angular speed that the sensor container will rotate per frame.
        :param sensor_name: The name of the target sensor.
        :param avatar_id: The ID of the avatar.
        """

        super().__init__(speed=speed, sensor_name=sensor_name, avatar_id=avatar_id)
        if position is None:
            """:field
            The target position to rotate towards.
            """
            self.position: Dict[str, float] = {"x": 0, "y": 0, "z": 0}
        else:
            self.position = position
