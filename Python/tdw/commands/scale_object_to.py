# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.object_command import ObjectCommand
from typing import Dict


class ScaleObjectTo(ObjectCommand):
    """
    Scale the object to the given value. This is only useful if you know the model scale beforehand, which is not always (1, 1, 1). This command is only useful when used with send_scales, because ObjectScales output data will return the actual scale of each object.
    """

    def __init__(self, id: int, scale: Dict[str, float] = None):
        """
        :param id: The unique object ID.
        :param scale: The object's new scale.
        """

        super().__init__(id=id)
        if scale is None:
            """:field
            The object's new scale.
            """
            self.scale: Dict[str, float] = {"x": 1, "y": 1, "z": 1}
        else:
            self.scale = scale
