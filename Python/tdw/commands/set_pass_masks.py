# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.avatar_command import AvatarCommand
from typing import List


class SetPassMasks(AvatarCommand):
    """
    Set which types of images the avatar will render. By default, the avatar will render, but not send, these images. See send_images in the Command API.
    """

    def __init__(self, pass_masks: List[str], avatar_id: str = "a"):
        """
        :param pass_masks: The avatar will render each of these passes per frame.
        :param avatar_id: The ID of the avatar.
        """

        super().__init__(avatar_id=avatar_id)
        """:field
        The avatar will render each of these passes per frame.
        """
        self.pass_masks: List[str] = pass_masks
