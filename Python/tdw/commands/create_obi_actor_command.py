# AUTOGENERATED FROM C#. DO NOT MODIFY.

from abc import ABC
from tdw.commands.obi_command import ObiCommand
from typing import Dict


class CreateObiActorCommand(ObiCommand, ABC):
    """
    These commands add Obi actor objects to the scene.
    """

    def __init__(self, position: Dict[str, float] = None, rotation: Dict[str, float] = None, id: int = 0, solver_id: int = 0):
        """
        :param position: The position of the Obi actor.
        :param rotation: The rotation of the Obi actor in Euler angles.
        :param id: The unique ID of the emitter.
        :param solver_id: The ID of the Obi solver.
        """

        super().__init__()
        if position is None:
            """:field
            The position of the Obi actor.
            """
            self.position: Dict[str, float] = {"x": 0, "y": 0, "z": 0}
        else:
            self.position = position
        if rotation is None:
            """:field
            The rotation of the Obi actor in Euler angles.
            """
            self.rotation: Dict[str, float] = {"x": 0, "y": 0, "z": 0}
        else:
            self.rotation = rotation
        """:field
        The unique ID of the emitter.
        """
        self.id: int = id
        """:field
        The ID of the Obi solver.
        """
        self.solver_id: int = solver_id
