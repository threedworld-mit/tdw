# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.adjust_textured_quad_command import AdjustTexturedQuadCommand
from typing import Dict


class ScaleTexturedQuad(AdjustTexturedQuadCommand):
    """
    Scale a textured quad by a factor.
    """

    def __init__(self, id: int, scale_factor: Dict[str, float] = None):
        """
        :param id: The ID of the non-physics object.
        :param scale_factor: Multiply the scale of the quad by this vector. (For example, if scale_factor is (2,2,2), then the quad's current size will double.)
        """

        super().__init__(id=id)
        if scale_factor is None:
            """:field
            Multiply the scale of the quad by this vector. (For example, if scale_factor is (2,2,2), then the quad's current size will double.)
            """
            self.scale_factor: Dict[str, float] = {"x": 1, "y": 1, "z": 1}
        else:
            self.scale_factor = scale_factor
