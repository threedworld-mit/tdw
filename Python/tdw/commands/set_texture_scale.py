# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.visual_material_command import VisualMaterialCommand
from typing import Dict


class SetTextureScale(VisualMaterialCommand):
    """
    Set the scale of the tiling of the material's main texture.
    """

    def __init__(self, id: int, object_name: str, scale: Dict[str, float] = None):
        """
        :param id: The unique object ID.
        :param object_name: The name of the sub-object.
        :param scale: The tiling scale of the material. Generally (but by no means always), the default tiling scale of a texture is {"x": 1, "y": 1}
        """

        super().__init__(object_name=object_name, id=id)
        if scale is None:
            """:field
            The tiling scale of the material. Generally (but by no means always), the default tiling scale of a texture is {"x": 1, "y": 1}
            """
            self.scale: Dict[str, float] = {"x": 1, "y": 1}
        else:
            self.scale = scale
