# AUTOGENERATED FROM C#. DO NOT MODIFY.

from tdw.commands.avatar_command import AvatarCommand
from typing import Dict


class ScaleAvatar(AvatarCommand):
    """
    Scale the avatar's size by a factor from its current scale.
    """

    def __init__(self, scale_factor: Dict[str, float] = None, avatar_id: str = "a"):
        """
        :param scale_factor: Multiply the scale of the avatar by this vector. (For example, if scale_factor is (2,2,2), then the avatar's current size will double.)
        :param avatar_id: The ID of the avatar.
        """

        super().__init__(avatar_id=avatar_id)
        if scale_factor is None:
            """:field
            Multiply the scale of the avatar by this vector. (For example, if scale_factor is (2,2,2), then the avatar's current size will double.)
            """
            self.scale_factor: Dict[str, float] = {"x": 1, "y": 1, "z": 1}
        else:
            self.scale_factor = scale_factor
